<main class="main-carrito">
    <section class="carrito">
        
        <a class="volver" href="/"><span class="flecha">&larr;</span> Volver</a> <!-- &larr es una flechita para que se vea en el despliegue-->
        
        <% if (productos.length != 0) { %> <!--si hay productos en el carrito, muestra la vista de cada producto individual con la información correspondiente de la bd junto con la cantidad de productos y el total por producto en puntos -->
            <h1>Tu carrito</h1>

            <% productos.forEach(producto => { %>
                <article class= "carrito-item">
                    <div class="carrito-item-info">
                        <img src="<%= producto.imagen %>" alt="<%= producto.nombre %>">
                        <h2><%= producto.nombre %></h2>
                    </div>
                    <div class="carrito-item-acciones">
                        <div class="modificador-cantidad">
                            <a href ="/carrito/restar/<%= producto.id %>">-</a>
                            <span class="cantidad"><%= producto.cantidad %></span>
                            <a href ="/carrito/sumar/<%= producto.id %>">+</a>
                        
                        </div>
                        <div class="precio-y-quitar">
                            <span class="precio"><%= producto.precio * producto.cantidad %> puntos</span>
                            <a class="boton-eliminar" href="/carrito/eliminar/<%= producto.id %>">🗑️</a>
                        </div>
                    </div>

                </article>
            <% }); %>
            <!-- footer del carrito donde se visualiza el total de todos los productos seleccionados y el total de todos, además del botón para redirigir al checkout o vaciar el carrito totalmente-->
            <div class="carrito-footer">
                <div class="resumen-caja">
                    <div class="total-fila">
                        <span class="total-etiqueta">Total a pagar:</span>
                        <span class="total-valor"><%= total %> puntos</span>
                    </div>
                    
                    <a href="/checkout" class="boton-finalizar">Finalizar compra</a>
                    
                    <a class="link-vaciar" href="/carrito/vaciar">Vaciar carrito</a>
                </div>
            </div>

            <!-- si el carrito no tiene productos, se muestra un parrafo indicando que esta vacío-->
            <% } else { %>
                <p id="carrito-vacio" >Tu carrito está vacío.</p>
            <% } %>
        
    </section>
</main>
